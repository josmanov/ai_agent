**main.py**
[x] remove temp name printing and switch to calling the call_function
[x] Raise excepetion if any of the following lists or properties are empty or None;
    - .parts list
    - parts.function_response property
    - .parts.function_response.response field
[X] set default working_directory to ./calculator
[X] if verbose is active print the response of each call
[X] create loop up until 20 iterations of replies.
[X] check for each canditate add the content of it into messages list
[X] append function results into messages with role set to user and parts as function_responses
[X] print response.text and exit ones theres no more responses
[X] exit(1) if no responses after max 20 iterations
[X] fix bug: specific files are not being read properly. 'None' is constantly retured.
    -  bug was located in get_file_content

**functions/get_files_info:**
[X] fix bug "pkg" is not a directory
[X] fix directory content being printed
[X] Fix error printed no matter what directory is accessed

**functions/get_file_content:**
[X] bug: return strings and content instead of printing

**functions/write_file:**
[X] Bug: Fix file generated in wrong directory.
    - Creates new directory in wrong path


**functions/get_file_info, write_file, get_files_content,:**
[X] Remove hardcoded "calculator" working directory. Use working_directory instead
[X] change all prints into returns

**functions/get_file_info, write_file, get_files_content, run_python_file**
[X] Use os.path.abspath to get absolute path of working_directory

**functions/write_file , get_files_content , run_python_file,:**
[X] Update all the new schemas to the corresponding function used.
    [x] write_file
    [x] get_file_content
    [x] run_python_file
[X] Update list of availabe functions to include all the declarations
[X] Update system prompt

**functions/call_function**
[x] Create a function to call the matching function with its arguments
    [x] Create function_map with all the commands
    [x] Create shallow copy of function_call
    [x] From args update the working_directory keys value into "./calculator"
    [x] Pass working dictionary into the callable function
    [x] Assign the result of a function call into a variable. 
        - The result will always be a string its defined in our callale functions